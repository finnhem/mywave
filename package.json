{
  "name": "mywave",
  "version": "1.0.0",
  "description": "Digital waveform viewer for VCD files",
  "scripts": {
    "build": "npx @rspack/cli build",
    "watch": "npx @rspack/cli build --watch",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.3",
    "@rspack/cli": "^1.3.0",
    "@rspack/core": "^1.3.0",
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
} 